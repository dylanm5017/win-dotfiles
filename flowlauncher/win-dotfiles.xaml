<!--
    win-dotfiles Flow Launcher theme.

    Colors are NOT hand-edited: the block between the win-dotfiles:theme markers is a set of
    palette brushes rewritten by the `theme` command from the active palette
    (themes/<name>.psd1 -> YasbPalette), exactly like yasb's :root and the VSCode overlay.
    Everything below references those brushes via {StaticResource wd*}, so the launcher
    restyles in lock-step with the bar, terminal, prompt, and desktop.

    Structure mirrors Flow's shipped themes: styles are BasedOn the Base* keys from Base.xaml;
    we only override the color-bearing setters. tools/Apply-FlowLauncher.ps1 copies this file
    into %APPDATA%\FlowLauncher\Themes and points Flow's Settings.json at it.
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:system="clr-namespace:System;assembly=mscorlib">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Themes/Base.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- win-dotfiles:theme:start -->
    <SolidColorBrush x:Key="wdBg" Color="#191724" />
    <SolidColorBrush x:Key="wdSurface" Color="#1f1d2e" />
    <SolidColorBrush x:Key="wdSurfaceAlt" Color="#26233a" />
    <SolidColorBrush x:Key="wdText" Color="#e0def4" />
    <SolidColorBrush x:Key="wdSubtext" Color="#908caa" />
    <SolidColorBrush x:Key="wdAccent" Color="#ebbcba" />
    <SolidColorBrush x:Key="ItemSelectedBackgroundColor" Color="#1f1d2e" />
    <!-- win-dotfiles:theme:end -->

    <Thickness x:Key="ResultMargin">0 0 0 4</Thickness>

    <Style x:Key="ItemGlyph" BasedOn="{StaticResource BaseGlyphStyle}" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource wdSubtext}" />
    </Style>

    <Style x:Key="QueryBoxStyle" BasedOn="{StaticResource BaseQueryBoxStyle}" TargetType="{x:Type TextBox}">
        <Setter Property="Foreground" Value="{StaticResource wdText}" />
        <Setter Property="CaretBrush" Value="{StaticResource wdAccent}" />
        <Setter Property="SelectionBrush" Value="{StaticResource wdAccent}" />
        <Setter Property="Height" Value="48" />
        <Setter Property="FontSize" Value="22" />
        <Setter Property="Padding" Value="0 0 66 0" />
    </Style>

    <Style x:Key="QuerySuggestionBoxStyle" BasedOn="{StaticResource BaseQuerySuggestionBoxStyle}" TargetType="{x:Type TextBox}">
        <Setter Property="Foreground" Value="{StaticResource wdSubtext}" />
        <Setter Property="Height" Value="48" />
        <Setter Property="FontSize" Value="22" />
        <Setter Property="Padding" Value="0 0 66 0" />
    </Style>

    <Style x:Key="WindowBorderStyle" BasedOn="{StaticResource BaseWindowBorderStyle}" TargetType="{x:Type Border}">
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{StaticResource wdSurfaceAlt}" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Background" Value="{StaticResource wdBg}" />
    </Style>

    <Style x:Key="WindowStyle" BasedOn="{StaticResource BaseWindowStyle}" TargetType="{x:Type Window}">
        <Setter Property="Width" Value="600" />
        <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled" />
    </Style>

    <Style x:Key="PendingLineStyle" BasedOn="{StaticResource BasePendingLineStyle}" TargetType="{x:Type Line}">
        <Setter Property="Stroke" Value="{StaticResource wdAccent}" />
        <Setter Property="Opacity" Value="0.6" />
    </Style>

    <!--  Result items  -->
    <Style x:Key="ItemTitleStyle" BasedOn="{StaticResource BaseItemTitleStyle}" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource wdText}" />
    </Style>
    <Style x:Key="ItemSubTitleStyle" BasedOn="{StaticResource BaseItemSubTitleStyle}" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource wdSubtext}" />
        <Setter Property="FontSize" Value="13" />
    </Style>
    <Style x:Key="ItemNumberStyle" BasedOn="{StaticResource BaseItemNumberStyle}" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource wdSubtext}" />
    </Style>
    <Style x:Key="ItemTitleSelectedStyle" BasedOn="{StaticResource BaseItemTitleSelectedStyle}" TargetType="{x:Type TextBlock}">
        <Setter Property="Cursor" Value="Arrow" />
        <Setter Property="Foreground" Value="{StaticResource wdAccent}" />
    </Style>
    <Style x:Key="ItemSubTitleSelectedStyle" BasedOn="{StaticResource BaseItemSubTitleSelectedStyle}" TargetType="{x:Type TextBlock}">
        <Setter Property="Cursor" Value="Arrow" />
        <Setter Property="Foreground" Value="{StaticResource wdSubtext}" />
    </Style>
    <Style x:Key="ItemImageSelectedStyle" BasedOn="{StaticResource BaseItemImageSelectedStyle}" TargetType="{x:Type Image}">
        <Setter Property="Cursor" Value="Arrow" />
    </Style>
    <Style x:Key="HighlightStyle" />
    <Style x:Key="ItemHotkeyStyle" BasedOn="{StaticResource BaseItemHotkeyStyle}" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource wdSubtext}" />
        <Setter Property="Opacity" Value="0.8" />
    </Style>
    <Style x:Key="ItemHotkeySelectedStyle" BasedOn="{StaticResource BaseItemHotkeySelectedStyle}" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource wdSubtext}" />
        <Setter Property="Opacity" Value="0.8" />
    </Style>

    <!--  Scrollbar thumb  -->
    <Style x:Key="ThumbStyle" BasedOn="{StaticResource BaseThumbStyle}" TargetType="{x:Type Thumb}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border
                        Background="{StaticResource wdSurfaceAlt}"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        CornerRadius="2"
                        DockPanel.Dock="Right" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="ScrollBarStyle" BasedOn="{StaticResource BaseScrollBarStyle}" TargetType="{x:Type ScrollBar}" />

    <Style x:Key="SeparatorStyle" BasedOn="{StaticResource BaseSeparatorStyle}" TargetType="{x:Type Rectangle}">
        <Setter Property="Fill" Value="{StaticResource wdSurfaceAlt}" />
        <Setter Property="Height" Value="1" />
        <Setter Property="Margin" Value="0 0 0 0" />
    </Style>

    <Style x:Key="SearchIconStyle" BasedOn="{StaticResource BaseSearchIconStyle}" TargetType="{x:Type Path}">
        <Setter Property="Fill" Value="{StaticResource wdSubtext}" />
        <Setter Property="Width" Value="32" />
        <Setter Property="Height" Value="32" />
    </Style>

    <Style x:Key="ClockBox" BasedOn="{StaticResource BaseClockBox}" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource wdSubtext}" />
    </Style>
    <Style x:Key="DateBox" BasedOn="{StaticResource BaseDateBox}" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource wdSubtext}" />
    </Style>

    <!--  Preview panel  -->
    <Style x:Key="PreviewBorderStyle" BasedOn="{StaticResource BasePreviewBorderStyle}" TargetType="{x:Type Border}">
        <Setter Property="BorderBrush" Value="{StaticResource wdSurfaceAlt}" />
        <Setter Property="Margin" Value="0 0 0 0" />
    </Style>
    <Style x:Key="PreviewItemTitleStyle" BasedOn="{StaticResource BasePreviewItemTitleStyle}" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource wdText}" />
    </Style>
    <Style x:Key="PreviewItemSubTitleStyle" BasedOn="{StaticResource BasePreviewItemSubTitleStyle}" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource wdSubtext}" />
    </Style>
    <Style x:Key="PreviewGlyph" BasedOn="{StaticResource BasePreviewGlyph}" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource wdText}" />
    </Style>
</ResourceDictionary>
